"""
Why use a Tuple?
It makes your code safer
Tuples are faster than lists
Valid keys in a dictionary
"""

first_tuple = (1, 2, 3, 3, 3)

print(first_tuple[1])
print(first_tuple[2])
print(first_tuple[-1])
